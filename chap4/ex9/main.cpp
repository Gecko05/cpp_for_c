struct a {
    int i, j, k;
};

// This class is not useful because data members
// are private by default. We need to add the public
// tag in order to be able to use its data members.
class a{ 
    int i, j, k;
};